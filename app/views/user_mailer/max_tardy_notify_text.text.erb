<% absences = Attendance.where(user_id: @user.id, event: @event.event_name, :absent => true).count%>
Limit on tardies reached, +1 absence
You now have <%= absences%>/<%= @event.absence_max%> allowed absences


