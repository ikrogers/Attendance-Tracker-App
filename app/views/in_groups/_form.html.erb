<%= form_for(@in_group) do |f| %>
  <% if @in_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@in_group.errors.count, "error") %> prohibited this in_group from being saved:</h2>

      <ul>
      <% @in_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
