<% if notice %>
<p id="notice">
	<%= notice %>
</p>
<% end %>

<h1>Homepage#index</h1>
<p>

</p>
<%= current_user.email %>

<% if current_user.admin == true %>
	<%= button_to "admin", admin_root_path, :method => :get %>
	<%= button_to "recall", new_message_path, :method => :get %>
	<%= button_to "Group and Attendace Management", groups_path, :method => :get %>
	<%= button_to "track recall confirmations(If applicable)", user_confirmations_path, :method => :get %>
<%end%>


<% if current_user.leader == true && current_user.admin == false %>
	
	<%= button_to "Take/View Attendance", groups_path, :method => :get %>
<% end %>

<%= button_to "logout", destroy_user_session_path, :method => :delete%>

<% %>
<% %>
<% %>
<% %>

<% @attendancept = Attendance.where(user_id: current_user.id, event: "PT")%>
<% @attendancellab = Attendance.where(user_id: current_user.id, event: "LLAB")%>


<br>Attendance Stats:<br>
Missed PT:
<%= @attendancept.count %>
<table>
<% @attendancept.each do |a|%>

	<th>
		Date missed:
	</th>
	<tr>
		<td>
			<%= a.created_at%>
		</td>
	</tr>

<% end %>
</table>
<br>
Missed LLAB:
<%= @attendancellab.count %>
<table>
<% @attendancellab.each do |a|%>

	<th>
		Date missed:
	</th>
	<tr>
		<td>
			<%= a.created_at%>
		</td>
	</tr>



<%end%>
</table>