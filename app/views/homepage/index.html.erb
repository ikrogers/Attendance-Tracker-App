<% if notice %>
<p id="notice">
	<%= notice %>
</p>
<% end %>

<h1>Homepage#index</h1>
<p>

</p>
<%= button_to "logout", destroy_user_session_path, :method => :delete%>
<%= current_user.email %>

<% if current_user.admin == true %>
	<%= button_to "admin", admin_root_path, :method => :get %>
	<%= button_to "recall", new_message_path, :method => :get %>
	<%= button_to "create/manage groups", groups_path, :method => :get %>
	<%= button_to "track confirmations(If applicable)", user_confirmations_path, :method => :get %>
<%end%>

<% if current_user.tracker == true %>
<% @leader = Group.find_by_users_id(current_user.id) rescue nil%>
	<% if @leader != nil%>
	<%= button_to "manage attendance", attendances_path, :method => :get %>
	<% end %>
<% end %>

<% if current_user.leader== true || current_user.tracker == true%>
	<%= button_to "create/manage groups", groups_path, :method => :get %>
<% %>
<% %>
<% end %>
<% %>
<% %>
<% %>
<% %>

<% @attendancept = Attendance.where(user_id: current_user.id, event: "PT")%>
<% @attendancellab = Attendance.where(user_id: current_user.id, event: "LLAB")%>


<br>Attendance Stats:<br>
Missed PT:
<%= @attendancept.count %>
<table>
<% @attendancept.each do |a|%>

	<th>
		Date missed:
	</th>
	<tr>
		<td>
			<%= a.created_at%>
		</td>
	</tr>

<% end %>
</table>
<br>
Missed LLAB:
<%= @attendancellab.count %>
<table>
<% @attendancellab.each do |a|%>

	<th>
		Date missed:
	</th>
	<tr>
		<td>
			<%= a.created_at%>
		</td>
	</tr>



<%end%>
</table>