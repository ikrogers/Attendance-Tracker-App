<div class="panel panel-primary">
	<div class="panel-heading">
		Excused Users
	</div>
	<div class="panel-body">
		<body id="content" role="main">
			<%= form_for(@excused_user) do |f| %>
			<% if @excused_user.errors.any? %>
			<div id="error_explanation">
				<h2><%= pluralize(@excused_user.errors.count, "error") %> prohibited this group from being saved:</h2>

				<ul>
					<% @excused_user.errors.full_messages.each do |message| %>
					<li>
						<%= message %>
					</li>
					<% end %>
				</ul>
			</div>
			<% end %>

			

			<div class="actions">
				<%= f.submit "Update Excused Users", :class=>'btn btn-primary' %>
			</div>
			<% end %>
	</div>
</div>

<script>
	$(document).ready(function() {
		$('select').selectpicker();
	}); 
</script>
