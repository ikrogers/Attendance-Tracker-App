<%= form_for(@excused_user) do |f| %>
  <% if @excused_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@excused_user.errors.count, "error") %> prohibited this excused_user from being saved:</h2>

      <ul>
      <% @excused_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
